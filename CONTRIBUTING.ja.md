# コントリビューションガイド

agd に関心を持っていただきありがとうございます。小さく方針のはっきりしたプロジェクトなので、
大きめの変更に着手する前にまず issue を立ててください(マージされない作業に時間を使わないために)。

[English version](CONTRIBUTING.md)

## 開発をはじめる

```bash
git clone https://github.com/wand125/agd.git
cd agd
bun install          # 開発用の依存のみ(typescript + @types/bun)
bun run web/server.ts # http://localhost:8787
```

サーバー本体は **実行時の依存パッケージがゼロ**(Bun のみ)です。この方針は維持したいので、
`dependencies` を増やす場合は相応の理由を書いてください。

## push する前に

```bash
bun run check        # 型チェック + テスト
```

どちらも CI で全 PR に対して実行されます。

## テストできる範囲

`bun test` が見ているのは純粋なロジックだけです(トランスクリプト解析、画面/プロンプト検出、
tmux 出力の解析)。それ以外(iTerm2 の Apple Events、tmux 操作、セッション検出)は
実際に動いている macOS のセッションが要るため、手動で確認しています。

この線引きはコードを追加するときに効いてきます。**「この文字列を渡したらこの値になる」と
書けるバグなら、`web/` 配下の純粋関数に切り出してテストを足してください。**
`web/screen.ts` はまさにそのために存在しています。

## 手動での確認

ターミナルに触る挙動は CI では守れません。その周辺を変更したときは、実際に何を確認したかを
PR に書いてください。壊れやすいのは次の経路です。

- **画面キャプチャ** — 素の iTerm2 タブと tmux ペインの両方でカードに画面が出るか
- **入力の送信** — `/api/send` がセッションに届き、Enter で確定されるか
- **応答ボタン** — 許可プロンプトがボタンとして描画され、押して応答できるか
- **セッションのライフサイクル** — 新規 / 複製(`Ctrl+N`)/ fork(`Ctrl+C`)/ 終了(`:q`)
- **リモートセッション** — `web/remote.ts` に触るなら実際の ssh + tmux ホストで確認する

知っておくと嵌まらない罠として、agd は launchd 配下では**最小の環境**で動きます。`LANG` が無く、
`PATH` も `/usr/bin:/bin:/usr/sbin:/sbin` まで削られます。手元のシェルとは挙動が変わり、
たとえば tmux はロケール未設定だとタブを `_` に置換します。外部バイナリは絶対パスで解決し、
区切り文字にタブを使わないでください。**対話シェルからの確認だけでは再現しません。**

## コードスタイル

既存のコードに合わせてください(整形し直さないこと)。

- **コメントは日本語**で、*何をしているか*ではなく*なぜそうしているか*を書きます。特に
  「その行が回避している非自明な制約」を残してください。新しいコメントも同じ調子で
- インデント2スペース、ダブルクォート、セミコロンあり
- フロントエンドにビルド工程はありません。`web/public/*.js` はブラウザが直接読む素の ES
  モジュールです。共通処理は `core.js`、PC 版 UI は `app.js`、モバイル版は `mobile.js`
- サーバーは関心ごとに1ファイル(`server.ts` / `remote.ts` / `transcript.ts` / `screen.ts` /
  `search-worker.ts`)。重い処理はメインスレッドの外に出します

## セキュリティ

agd はあなたのターミナルに文字を打ち込めます。バインド・認証・API の範囲に関わる変更をする前に
[SECURITY.md](SECURITY.md) を読んでください。**脆弱性は公開 issue に書かないでください。**
