# セキュリティポリシー

[English version](SECURITY.md)

## 脆弱性の報告

セキュリティ上の問題は **公開 issue に書かないでください。**

[GitHub の非公開の脆弱性報告](https://github.com/wand125/agd/security/advisories/new)
を使ってください。攻撃者に何ができるのかと、再現手順を書いてください。個人プロジェクトのため、
返信は数時間ではなく数日単位とお考えください。

## 想定する脅威

agd は **あなたのターミナルセッションに文字を打ち込めます。** この HTTP サーバーに到達できる者は、
起動中のすべてのエージェントにキー入力を送れます。つまり実質的にあなたのマシンでコマンドを
実行できます。**このポートへのアクセスはシェルへのアクセスと同等**に扱ってください。

既定値はこの前提で決まっています。

- サーバーは **`127.0.0.1` のみ**にバインドします。`AGD_BIND` で変更できますが、
  **`AGD_TOKEN` 無しで外部にバインドすると起動を拒否します**
- `AGD_TOKEN` を設定すると、トークンの無いリクエストは 401 になります。**localhost からの
  リクエストも含みます。** ループバックを意図的に免除していません — `tailscale serve` のような
  リバースプロキシも `127.0.0.1` から接続してくるため、免除するとトークンが無意味になります
- `AGD_READONLY=1` はすべての更新系エンドポイントを拒否します(閲覧専用で公開したいとき用)

## localhost の外に出す場合

別の端末から使いたい場合は、プライベートネットワーク(tailnet)に載せ、**かつ**トークンを
設定してください。**インターネットへのポート開放はしないでください。**

```bash
AGD_TOKEN=$(openssl rand -hex 24) agd web
tailscale serve --bg --http=8787 http://127.0.0.1:8787
```

トークンは初回だけ URL に現れ(`?token=…`)、以降は Cookie に保存されます。ブラウザの履歴に
残るため、気になる場合はローテーションしてください。

## 対象範囲

対象: 認証のバイパス、本来届かないはずのセッションにリクエストが届いてしまうもの、
セッション由来のデータ(パス・タイトル・トランスクリプト内容)経由のコマンドインジェクション、
ログやレスポンスへのトークン漏洩。

対象外: 同一ユーザーのローカルシェルをすでに取得していることが前提の攻撃(この道具は設計上
その権限を前提としています)、および Claude Code / Codex CLI 自体のセキュリティ(上流に報告してください)。
